import chalk from "chalk";

const colors = {
    'error': chalk.red,
    'sucess': chalk.green,

    'pending': chalk.yellow,
    'progress': chalk.blue,
    'complete': chalk.green,
}

export {colors as chalk_colors}
